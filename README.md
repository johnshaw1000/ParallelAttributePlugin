# ParallelAttributePlugin
SpecFlow plugin for running tests in parallel

This plugin makes use of the NUnit ParallelAttribute to allow tests to run in parallel. Requires NUnit 3 (currently a beta release).
